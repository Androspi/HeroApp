import { LastNumberPipe } from './last-number.pipe';

describe('LastNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new LastNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
